// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // apiUrl: 'https://testapi.raheelaftab.com/',
  // apiUrl: 'https://localhost:44346/',
  // apiUrl: 'https://connectposapi.technospiders.net/',
  // apiUrl: 'https://almonas-api.technospiders.net/',
  // apiUrl: 'https://flylinkcourier-api.technospiders.net',
  apiUrl: 'https://huzaifatraders.technospiders.net/',
  // apiUrl: '/',
  allowFileExtension: ['pdf', 'doc', 'docx', 'xls', 'xlsx'],
  allowExtesions: ['pdf', 'doc', 'docx'],
  maximumFileSize: 104857600
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
